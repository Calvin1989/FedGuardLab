<!DOCTYPE html>
<html lang="{{ lang }}">
<head>
  <meta charset="UTF-8" />
  <title>{{ labels.title }} - {{ job_id }}</title>
  <style>
    body {
      margin: 0;
      background: #f8fafc;
      color: #0f172a;
      font-family:
        Inter,
        ui-sans-serif,
        system-ui,
        -apple-system,
        BlinkMacSystemFont,
        "Segoe UI",
        sans-serif;
    }

    .container {
      max-width: 1120px;
      margin: 40px auto;
      padding: 40px;
      background: #ffffff;
      border: 1px solid #e2e8f0;
      border-radius: 18px;
      box-shadow: 0 24px 60px rgba(15, 23, 42, 0.08);
    }

    .report-kicker {
      color: #64748b;
      font-size: 14px;
      font-weight: 700;
      letter-spacing: 0.04em;
      text-transform: uppercase;
    }

    h1 {
      margin: 10px 0 18px;
      font-size: 36px;
      line-height: 1.1;
    }

    h2 {
      margin-top: 32px;
      margin-bottom: 16px;
      font-size: 22px;
    }

    .metric-grid {
      display: grid;
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: 16px;
      margin: 24px 0;
    }

    .metric-card {
      padding: 18px;
      border: 1px solid #e2e8f0;
      border-radius: 14px;
      background: #f8fafc;
    }

    .metric-label {
      display: block;
      color: #64748b;
      font-size: 13px;
      font-weight: 700;
    }

    .metric-value {
      display: block;
      margin-top: 8px;
      font-size: 28px;
      font-weight: 800;
    }

    .overview-table {
      width: 100%;
      border-collapse: collapse;
      overflow: hidden;
      border: 1px solid #e2e8f0;
      border-radius: 12px;
    }

    .overview-table th,
    .overview-table td {
      padding: 12px 14px;
      border-bottom: 1px solid #e2e8f0;
      text-align: left;
    }

    .overview-table th {
      width: 35%;
      background: #f1f5f9;
    }

    .export-grid {
      display: grid;
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: 12px;
      margin-bottom: 16px;
    }

    .export-card {
      padding: 14px;
      border: 1px solid #e2e8f0;
      border-radius: 12px;
      background: #f8fafc;
    }

    .export-card strong {
      display: block;
      font-size: 14px;
    }

    .export-card span {
      display: block;
      margin-top: 6px;
      color: #64748b;
      font-size: 13px;
    }

    .config-details {
      margin-top: 16px;
      border: 1px solid #e2e8f0;
      border-radius: 14px;
      background: #f8fafc;
      overflow: hidden;
    }

    .config-details summary {
      cursor: pointer;
      padding: 16px 18px;
      font-weight: 800;
    }

    pre {
      margin: 0;
      padding: 20px;
      overflow-x: auto;
      background: #0f172a;
      color: #e2e8f0;
      font-size: 13px;
      line-height: 1.55;
    }

    .metrics-table {
      width: 100%;
      border-collapse: collapse;
      font-size: 14px;
    }

    .metrics-table th,
    .metrics-table td {
      padding: 10px 12px;
      border-bottom: 1px solid #e2e8f0;
      text-align: right;
    }

    .metrics-table th:first-child,
    .metrics-table td:first-child {
      text-align: left;
    }

    .metrics-table th {
      background: #f1f5f9;
    }

    .label-counts {
      font-family:
        ui-monospace,
        SFMono-Regular,
        Menlo,
        Monaco,
        Consolas,
        "Liberation Mono",
        monospace;
      font-size: 12px;
      color: #334155;
    }
  </style>
</head>
<body>
  <div class="container">
    <p class="report-kicker">{{ labels.title }}</p>
    <h1>{{ experiment_name }}</h1>

    <p><strong>{{ labels.job_id }}：</strong> {{ job_id }}</p>
    <p><strong>{{ labels.status }}：</strong> {{ status }}</p>

    <div class="metric-grid">
      <div class="metric-card">
        <span class="metric-label">{{ labels.final_round }}</span>
        <strong class="metric-value">{{ final_metric.round }}</strong>
      </div>

      <div class="metric-card">
        <span class="metric-label">{{ labels.final_accuracy }}</span>
        <strong class="metric-value">{{ final_metric.accuracy }}</strong>
      </div>

      <div class="metric-card">
        <span class="metric-label">{{ labels.final_loss }}</span>
        <strong class="metric-value">{{ final_metric.loss }}</strong>
      </div>

      <div class="metric-card">
        <span class="metric-label">{{ labels.final_asr }}</span>
        <strong class="metric-value">{{ final_metric.attack_success_rate }}</strong>
      </div>
    </div>

    <h2>{{ labels.overview }}</h2>

    <table class="overview-table">
      <tbody>
        <tr>
          <th>{{ labels.trainer }}</th>
          <td>{{ final_metric.trainer }}</td>
        </tr>
        <tr>
          <th>{{ labels.mode }}</th>
          <td>{{ final_metric.mode }}</td>
        </tr>
        <tr>
          <th>{{ labels.dataset }}</th>
          <td>{{ final_metric.dataset }}</td>
        </tr>
        <tr>
          <th>{{ labels.partition }}</th>
          <td>{{ final_metric.partition }}</td>
        </tr>
        <tr>
          <th>{{ labels.clients }}</th>
          <td>{{ final_metric.num_clients }}</td>
        </tr>
        <tr>
          <th>{{ labels.malicious_clients }}</th>
          <td>{{ final_metric.malicious_clients }}</td>
        </tr>
        <tr>
          <th>{{ labels.aggregation }}</th>
          <td>{{ final_metric.aggregation }}</td>
        </tr>
        <tr>
          <th>{{ labels.attack }}</th>
          <td>{{ final_metric.attack }}</td>
        </tr>
        <tr>
          <th>{{ labels.defense }}</th>
          <td>{{ final_metric.defense }}</td>
        </tr>
        <tr>
          <th>{{ labels.device }}</th>
          <td>{{ final_metric.device }}</td>
        </tr>
      </tbody>
    </table>

    {% if final_metric.client_label_summary is defined %}
    <h2>{{ labels.client_distribution }}</h2>

    <table class="metrics-table">
      <thead>
        <tr>
          <th>{{ labels.client_id }}</th>
          <th>{{ labels.samples }}</th>
          <th>{{ labels.label_counts }}</th>
        </tr>
      </thead>
      <tbody>
        {% for client in final_metric.client_label_summary %}
        <tr>
          <td>{{ client.client_id }}</td>
          <td>{{ client.num_samples }}</td>
          <td class="label-counts">{{ client.label_counts }}</td>
        </tr>
        {% endfor %}
      </tbody>
    </table>
    {% endif %}

    <h2>{{ labels.exported_files }}</h2>

    <div class="export-grid">
      <div class="export-card">
        <strong>config.json</strong>
        <span>{{ labels.export_config }}</span>
      </div>

      <div class="export-card">
        <strong>metrics.json</strong>
        <span>{{ labels.export_metrics_json }}</span>
      </div>

      <div class="export-card">
        <strong>metrics.csv</strong>
        <span>{{ labels.export_metrics_csv }}</span>
      </div>

      <div class="export-card">
        <strong>report.md</strong>
        <span>{{ labels.export_report_md }}</span>
      </div>
    </div>

    <details class="config-details" open>
      <summary>{{ labels.config_details }}</summary>
      <pre>{{ config_json }}</pre>
    </details>

    <h2>{{ labels.metrics }}</h2>
    <table class="metrics-table">
      <thead>
        <tr>
          <th>{{ labels.round }}</th>
          <th>{{ labels.accuracy }}</th>
          <th>{{ labels.loss }}</th>
          <th>{{ labels.asr }}</th>
        </tr>
      </thead>
      <tbody>
        {% for item in metrics %}
        <tr>
          <td>{{ item.round }}</td>
          <td>{{ item.accuracy }}</td>
          <td>{{ item.loss }}</td>
          <td>{{ item.attack_success_rate }}</td>
        </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>
</body>
</html>

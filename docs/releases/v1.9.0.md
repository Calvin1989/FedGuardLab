# FedGuardLab v1.9.0

Final release for v1.9.0 Experiment Result Management.

## Summary

v1.9.0 completes the Experiment Result Management release line. It focuses on making experiment outputs easier to manage, inspect, compare, archive, and clean up locally.

## Completed scope

- Dashboard history experiment management UI.
- Job archive / restore workflow.
- Archived job filtering and comparison protection.
- Comparison report history API.
- Comparison report history Dashboard panel.
- Unified report and artifact entry styling.
- Reports cleanup summary API.
- Reports cleanup summary Dashboard panel.
- Reports cleanup run API with safe dry-run default.
- Dashboard cleanup run controls with explicit confirmation for real deletion.
- v1.9 alpha, beta, and release candidate readiness documentation.

## Reports cleanup safety model

- `GET /reports/cleanup/summary` is read-only.
- `POST /reports/cleanup/run` defaults to `dry_run=true`.
- Real deletion requires `dry_run=false` and `confirm=true`.
- The Dashboard cleanup button requires browser confirmation before real deletion.
- Cleanup only acts on backend-generated cleanup preview candidates.
- Path safety checks reject reports root deletion and paths outside the reports root.

## Validation

Final release validation should include:

- `git diff --check`
- `python -m ruff check .`
- `python quick_test.py`
- `python -m pytest`
- `cd web && npm run build`
- `python api_smoke_test.py`
- Docker Compose config/build/up/down
- wait-finished smoke run
- backend restart recovery check
- GitHub Actions CI
- GitHub Actions Docker Smoke manual workflow

## Compatibility notes

v1.9.0 does not modify training core logic, does not break existing report artifact URLs, and does not require a database or external service.

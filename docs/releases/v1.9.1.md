# FedGuardLab v1.9.1

Maintenance release for v1.9.0.

## Summary

v1.9.1 is a maintenance-only release after v1.9.0. It records the GitHub Actions workflow update for Node 24 compatibility and confirms the Docker Smoke workflow after the workflow maintenance change.

## Completed scope

- Updated GitHub Actions workflow actions for Node 24 compatibility.
- Removed the Node.js 20 deprecation warning from future CI / Docker Smoke runs.
- Re-ran GitHub Actions Docker Smoke after the workflow update.
- Kept the v1.9.0 Experiment Result Management runtime scope unchanged.

## Validation

Validation for this maintenance release includes:

- `git diff --check`
- `python -m ruff check .`
- `python quick_test.py`
- `python -m pytest`
- `cd web && npm run build`
- `python api_smoke_test.py`
- GitHub Actions CI
- GitHub Actions Docker Smoke manual workflow

## Compatibility notes

v1.9.1 does not add runtime features, does not change API behavior, does not modify training core logic, does not change report artifact URLs, and does not add dependencies.
